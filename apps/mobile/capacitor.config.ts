import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.turnidipalco.mobile',
  appName: 'Turni di Palco',
  webDir: 'build',
};

export default config;
